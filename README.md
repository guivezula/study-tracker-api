# study-tracker-api
A small full-stack learning project focused on backend architecture and API design. The goal is to build a clean and scalable API that exposes the same domain model through both REST and GraphQL, using Node.js, Express and Prisma ORM.
